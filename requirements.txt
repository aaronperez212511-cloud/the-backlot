# Full local/dev set: the served app plus the data generation, loading and
# verification scripts. The Cloud Run image installs requirements-runtime.txt
# instead, which omits the data tooling.
-r requirements-runtime.txt

numpy>=2.5,<3
pandas>=3.0,<4
clickhouse-connect>=1.7,<2
